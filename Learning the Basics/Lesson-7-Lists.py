#--LISTS--
#Ordered, changeable, allows duplicates
fruits = ["pawpaw", "pear", "banana", "pear"]
print(fruits)
print(len(fruits))

#checking if an item exists in a list
if "pear" in fruits:
  print("Yes, 'pear' is in the fruit lists")

#list constructor can also be used to create lists
items = list(("Car", 35, True, 12.0))
print(items)

